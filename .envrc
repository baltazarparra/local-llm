# direnv configuration for automatic virtual environment activation
# This file tells direnv to activate the Python virtual environment
# when you cd into this directory.
#
# Setup instructions:
# 1. Install direnv: sudo apt install direnv (or brew install direnv on macOS)
# 2. Add hook to your shell:
#    - For zsh: echo 'eval "$(direnv hook zsh)"' >> ~/.zshrc
#    - For bash: echo 'eval "$(direnv hook bash)"' >> ~/.bashrc
# 3. Restart your shell or run: source ~/.zshrc (or ~/.bashrc)
# 4. In this directory, run: direnv allow .
#
# After setup, the virtual environment will automatically activate
# when you enter this directory and deactivate when you leave.

source .venv/bin/activate
